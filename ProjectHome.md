A Bourne shell script which utilizes bc (calculator), grep and stat to calculate the total size of a file or directory.  Works on Linux and BSD (set BSD=true).

Licensed under the ZLib/LibPNG license.